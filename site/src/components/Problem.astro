---
---
<section class="section container">
  <p class="eyebrow reveal">The problem</p>
  <div class="prose reveal mt-5 text-[1.2rem] leading-[1.7] md:text-[1.35rem]">
    <p>
      Three weeks ago you and Claude spent two hours working out why the Kie.ai endpoint
      returns 200 with the error in the body. Today it happens again, on a different
      project. Neither of you remembers. The session that solved it was pruned eleven days
      ago, and the fix exists nowhere — not in the code, not in a commit message, not in
      CLAUDE.md.
    </p>
  </div>
  <p class="reveal mt-10 max-w-[62ch] border-l-2 border-accent pl-5 text-ink-muted">
    vir is not a memory layer your agent writes to. It reads what already happened.
  </p>
</section>
